# Copy to desktop/.env.local (gitignored) when using `npm run dev:mock:live`.
# Optional override. If unset, the Vite proxy reads the desktop
# credentials.json already saved by the real app. The browser never sees it.
WORKBENCH_API_KEY=
