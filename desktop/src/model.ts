export type SectionId =
  "router" | "agents" | "api-keys" | "usage" | "logs" | "settings";

export const navigationItems: SectionId[] = [
  "router",
  "agents",
  "api-keys",
  "usage",
  "logs",
  "settings",
];
